import { ColornotedemoDirective } from './colornotedemo.directive';

describe('ColornotedemoDirective', () => {
  it('should create an instance', () => {
    const directive = new ColornotedemoDirective();
    expect(directive).toBeTruthy();
  });
});
